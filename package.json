{
  "name": "cortex-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter \"./apps/*\" --parallel dev",
    "api": "pnpm --filter api-server",
    "web": "pnpm --filter web-dashboard"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
